apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: openfga-db-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: openfga-db-credentials
  data:
    - secretKey: db-user
      remoteRef:
        key: secret/data/api-security/openfga-db
        property: username
    - secretKey: db-password
      remoteRef:
        key: secret/data/api-security/openfga-db
        property: password
