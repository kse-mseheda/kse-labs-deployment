apiVersion: v1
kind: Service
metadata:
  name: dpop-rs
  namespace: applications
  labels:
    app.kubernetes.io/name: dpop-rs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: dpop-rs
