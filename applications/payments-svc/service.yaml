apiVersion: v1
kind: Service
metadata:
  name: payments-svc
  namespace: applications
  labels:
    app.kubernetes.io/name: payments-svc
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: payments-svc
