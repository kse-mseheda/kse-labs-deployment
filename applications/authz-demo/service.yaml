apiVersion: v1
kind: Service
metadata:
  name: authz-demo
  namespace: applications
  labels:
    app.kubernetes.io/name: authz-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: authz-demo
